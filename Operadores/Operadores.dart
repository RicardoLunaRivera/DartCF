void main(List<String> args) {
  int edadMax = 30;
  int edadmin = 18;
  int edad = 15;

  //Operadores de comparación >,<, <=, >=, ==, !=

  //Operadores aritméticos +, - , *, /, %
  //En  divisio si no se quieren lo desimales e ocupa -> ~/

  //Operadoes de asiganación =, +=, -=, *=, /=, ~/, %=

  //Operadores lógicos or=||, and= &&, not !

  //Operadores Unarios
  //Preincremento ++a
  //Predecremento --a
  //Postincrenento a++
  //Postdecremento a--

  //Operador ternario ejem->
  print((edad >= edadmin) ? "Puedes entrar" : "No puedes entrar");
}
